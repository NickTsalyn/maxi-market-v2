export interface Category {
  categoryName: string;
  imgUrl: string;
}

export interface ProductCard {
  name: string;
  imgUrl: string;
  description: string;
  price: string;
}

export interface ProductCardFilter {
  name: string;
  imgUrl: string;
  description: string;
  price: string;
}
